# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
htmlcov/
.coverage

# Node
node_modules/
.next/
out/
*.tsbuildinfo

<<<<<<< HEAD
# Desktop app: staged Python runtime + download caches (desktop/scripts/build-runtime.ps1)
desktop/build/
# setuptools output from non-editable installs of the backend
backend/build/
=======
# Desktop app: staged Python runtime + download caches (desktop/scripts/build-runtime.ps1 / .sh)
desktop/build/
# setuptools output from non-editable installs of the backend
backend/build/
Claude output/
>>>>>>> d325f0ffea140b14d8efde51c7c0cf3c0712f39b

# Env
.env
.env.local
.env.*.local

# Data / large files
data/
*.fits
*.FITS
*.fts
# Note: the ML checkpoint backend/ml_model/best.pt is intentionally NOT ignored —
# it is committed via Git LFS (see .gitattributes). Other *.pt files under the
# gitignored data/ and .venv/ trees are already excluded by those rules.

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.claude/
